describe('Database Connection', () => {
  test('URL POSTGRESQL testing 1', () => {
    const resultado = 2 + 2;
    expect(resultado).toBe(4);
  });
});
describe('Testing authentication', () => {
  test('Testing auth', () => {
    const resultado = 2 + 2;
    expect(resultado).toBe(4);
  });
});
describe('Testing', () => {
  test('Testing', () => {
    const resultado = 2 + 2;
    expect(resultado).toBe(4);
  });
});
describe('Testing', () => {
  test('Testing ', () => {
    const resultado = 2 + 2;
    expect(resultado).toBe(4);
  });
});
